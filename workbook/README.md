Drop the workbook you want reviewed in this folder (e.g. Book.xlsm).

Everything here except this file is gitignored, so the binary never gets
committed by accident and you don't have to think about paths - the tools and
the /workbook-review skill look here first.
